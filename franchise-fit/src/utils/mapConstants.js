/** DOM id on the map wrapper — used by mapCapture + MapView */
export const MAP_CAPTURE_ROOT_ID = 'franchisefit-map-root';

/** Dispatched before PDF snapshot so Leaflet can invalidateSize */
export const BEFORE_MAP_CAPTURE = 'franchisefit-before-map-capture';
